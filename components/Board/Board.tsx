import { Box } from '@mui/material';

const Board = () => {
  return (
    <Box
      id="play-board"
      sx={{ height: '100%', display: 'flex', flexDirection: 'column' }}
    ></Box>
  );
};

export default Board;
